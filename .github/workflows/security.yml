name: Security & License Audit

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    # Run weekly on Monday at 02:00 UTC
    - cron: '0 2 * * 1'

jobs:
  security-audit:
    name: CVE Vulnerability Scan (pip-audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Install System Dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y portaudio19-dev || true
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Run CVE Vulnerability Audit
        run: |
          python tools/run_security_audit.py

  license-check:
    name: Dependency License Compliance
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Install System Dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y portaudio19-dev || true
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Run License Compliance Check
        run: |
          python tools/run_license_check.py
